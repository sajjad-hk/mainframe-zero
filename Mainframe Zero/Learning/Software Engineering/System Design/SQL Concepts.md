# SQL Concepts
- Index: Is a data a structure that helps you quickly look up data in database tables. Indices in SQL enhance speed of accessing the data they eliminate the need to read through the whole table to locate the information needed.
	- Unique index
	- Filtered index (has very a few distinct values)
	- Hash index (different bucket of slots each of which contains a pointer that points to each row in the bucket)